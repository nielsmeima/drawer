[package]
name = "drawer"
version = "0.1.0"
edition = "2024"

[dependencies]
latest = { path = "../latest" }
clap = { version = "4", features = ["derive"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
anyhow = "1"

[build-dependencies]
latest = { path = "../latest" }
